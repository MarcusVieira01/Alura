//Loop for com arrays
//Declaração de variável e atribuição dos valores de uma lista
const numeros = [100, 200, 300, 400, 500, 600];
//Loop for que exibirá cada elemento da lista, sendoo índice o 
//valor da variável de controle i
for(let i = 0; i < numeros.length; i++){
    //Exibição via console.log(par) do valor da variável i e do
    //valor do índice i da lista números
    console.log(i, numeros[i]);
}
